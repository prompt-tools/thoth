# Domain Docs

This is a single-context repository.

Before exploring, read `CONTEXT.md` if it exists and any relevant decisions in `docs/adr/`. If they do not exist, proceed silently. Create them lazily when domain terms or architectural decisions need to be recorded.
